## Summary

Briefly describe what this PR changes and why.

## Related issue

Closes #

## Testing

- [ ] `go test ./...` passes
- [ ] `gofmt -l .` prints nothing
- [ ] `go vet ./...` is clean
- [ ] If the in-browser runner (`cmd/wasm`) changed, `public/runner.wasm.gz` was
      rebuilt with `./scripts/build-wasm.sh`

## Checklist

- [ ] Changes are focused and self-contained
- [ ] Documentation / CHANGELOG updated if behavior changed
